`SELECT * FROM table_name;`
- Select all records from table
`SELECT DISTINCT email FROM users;`
- Selects all distinct emails from users table
`UPDATE users SET email = 'email@email.com' WHERE first_name = 'John';`
- Updates in place where condition matches
